from django.urls import path

app_name = 'authentication'
urlpatterns = [
    # path('register/', CustomRegisterView.as_view()),
    # path('resend-verification/', ResendEmailActivation.as_view()),
    # path('register/verify-email/', CustomEmailVerify.as_view()),
    # path('token/', CustomTokenView.as_view(), name="token"),
    # path('token/revoke/', RevokeTokenView.as_view(), name='revoke-token'),
    # path('user/', CustomUserDetailsView.as_view(), name='user'),
    # path('email/change/', ChangeEmailView.as_view(), name='change_email'),
    # # path('social-accounts/', SocialAccountListView.as_view(), name='social_accounts'),
    # # path('social-accounts/connect/google/', GoogleConnectView.as_view(), name='social_account_connect_google'),
    # # path('social-accounts/<pk>/disconnect/', CustomSocialAccountDisconnectView.as_view(), name='social_disconnect'),
    # path('password/validate/', CheckPasswordView.as_view(), name='validate_password'),
    # path('inactive/', AccountInactiveView.as_view(), name='account_inactive'),
    # path('disable/', DisableAccountView.as_view(), name='disable'),
    # path('delete/', DeleteAccountView.as_view(), name='delete'),
]
